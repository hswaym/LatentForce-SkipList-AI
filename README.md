SkipList is a pre-migration code-triage tool that analyzes legacy Python codebases to identify dead and duplicate code, allowing migration teams to scope real work first. Run `skiplist analyze <path>` with optional flags `--entry`, `--exclude`, `--out`, and `--format` to scan a repository and generate triage reports.
